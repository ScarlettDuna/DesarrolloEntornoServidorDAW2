{% extends 'base.html.twig' %}

{% block body %}
<h1>Supplier </h1>

    <table>
        <tbody>
        <tr>
            <th>ID</th>
            <td>{{ product.id }}</td>
        </tr>
        <tr>
            <th>Name</th>
            <td>{{ product.name }}</td>
        </tr>
        <tr>
            <th>Address</th>
            <td>{{ product.address }}</td>
        </tr>
        </tbody>
    </table>
    <a href="{{ path('supplier_edit', {'id': product.id}) }}">Edit</a>
    <a href="{{ path('supplier_delete', {'id': product.id}) }}">Delete</a>

{% endblock %}
